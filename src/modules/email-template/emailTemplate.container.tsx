import React, { useState } from 'react';
function EmailTemplateContainer(): JSX.Element {
  const [refresh, setRefresh] = useState(false);


  return (
    <div>
      <h4>Manage Email Templates </h4>

    </div>
  );
}

export default EmailTemplateContainer;
